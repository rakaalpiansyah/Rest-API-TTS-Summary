# ─── Web Framework ───────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
websockets==12.0

# ─── AI — Speech to Text (Groq Cloud API) ────────────────────
# Tidak perlu faster-whisper lagi, cukup httpx untuk HTTP call
requests==2.31.0

# ─── AI — LLM via Groq (OpenAI-compatible) ───────────────────
openai==1.30.1

# ─── Database (Supabase) ─────────────────────────────────────
supabase==2.4.6
python-dotenv==1.0.1

# ─── Utilities ───────────────────────────────────────────────
pydantic==2.6.4
pydantic-settings==2.2.1
httpx==0.27.0